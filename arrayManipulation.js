function arrayManipulation(n, queries) {
    // Write your code here
    let inputRows = n.split("\n");
    let mDimensions=inputRows[0]; // matrix dimensions
    let queries=[[inputRows[1],inputRows[2],inputRows[3]]];
    let mWidth=mDimensions[0], mDepth=mDimensions[1];

    function constructMatrix(width,depth) {
        return 
    }
}

// arrayManipulation("5,3/n1,2,100/n3,4,100");

arrayManipulation(
`5 3
1 2 100
2 5 100
3 4 100`
);